/** App-wide feature flags. Flip to re-enable hidden surfaces. */

/** Projects page + its nav entry, add-menu item and home panel.
 *  Hidden while experimenting whether the projects layer is needed. */
export const SHOW_PROJECTS = false;
