# Mobile app — product scope

**This folder is the product.** The Next.js website is frozen until further notice.

- Implement features here (`mobile/`).
- Shared API / `lib` / Supabase may change only to support this app.
- Do not edit website UI for parity or convenience.

See root `CLAUDE.md` and `.cursor/rules/app-only-scope.mdc`.
